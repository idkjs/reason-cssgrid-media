/** plugin is also included in this configuration to 
 * automatically add vendor prefixes for only the browsers 
 * that need to be supported.  */
module.exports = {
  plugins: [require("autoprefixer")]
};
